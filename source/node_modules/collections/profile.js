'use strict';

let inherits = require('util').inherits,
  Collection = require('classes/collection');

function ProfileCollection() {
  Collection.call(this);
}

inherits(ProfileCollection, Collection);

module.exports = ProfileCollection;
