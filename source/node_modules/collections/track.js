'use strict';

let inherits = require('util').inherits,
  Collection = require('classes/collection');

function TrackCollection() {
  Collection.call(this);
}

inherits(TrackCollection, Collection);

module.exports = TrackCollection;
