'use strict';

let inherits = require('util').inherits,
  Collection = require('classes/collection');

function OptionsCollection() {
  Collection.call(this);
}

inherits(OptionsCollection, Collection);

module.exports = OptionsCollection;
