'use strict';

let inherits = require('util').inherits,
  Collection = require('classes/collection');

function ModulesCollection() {
  Collection.call(this);
}

inherits(ModulesCollection, Collection);

module.exports = ModulesCollection;
