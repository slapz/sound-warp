'use strict';

let inherits = require('util').inherits,
  Collection = require('classes/collection');

function PlaylistCollection() {
  Collection.call(this);
}

inherits(PlaylistCollection, Collection);

module.exports = PlaylistCollection;
