'use strict';

let props = Object.defineProperties;

function __define_const(value) {
  return {
    configurable: false,
    enumerable: true,
    writable: false,
    value: value
  };
}

function Artwork(url) {
  props(this, {
    url: {
      configurable: false,
      enumerable: true,
      writable: true,
      value: null
    }
  });

  this.setUrl(url);
}

props(Artwork, {
  SIZE_500: __define_const('t500x500'),
  SIZE_400: __define_const('crop'),
  SIZE_300: __define_const('t300x300'),
  SIZE_100: __define_const('large'),
  SIZE_67: __define_const('t67x67'),
  SIZE_47: __define_const('badge'),
  SIZE_32: __define_const('small'),
  SIZE_20: __define_const('tiny'),
  SIZE_16: __define_const('mini')
});

Artwork.prototype.getUrl = function(size) {
  return (this.data.get('url') || '')
    .replace(Artwork.SIZE_100, size || Artwork.SIZE_100);
};

module.exports = Artwork;
