'use strict';
/**
 * Extend objects
 */

let has = Object.prototype.hasOwnProperty;

module.exports = function(obj1, obj2) {
  Object.keys(obj2).forEach(function(index) {
    if (has.call(obj1, index) === false) {
      obj1[index] = obj2[index];
    }
  });
  return obj1;
};
